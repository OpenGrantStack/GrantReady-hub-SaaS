
---

# app/webhooks/grants.py

`python
from fastapi import APIRouter, Request, Header
from app.utils.verifysignature import verifysignature
from app.utils.logger import logger

router = APIRouter()


@router.post("/webhooks/grants")
async def grants_webhook(
    request: Request,
    xhubsignature_256: str = Header(None)
):
    body = await request.body()
    verifysignature(body, xhubsignature256, "YOURWEBHOOKSECRET")

    logger.info("Grants webhook received")
    # TODO: Link to grant application / review workflows
    return {"status": "ok", "webhook": "grants"}
`